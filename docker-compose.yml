# Server
PORT=3000
DOMAIN=localhost:3000
API_QUOTA=100

# Redis (local Docker)
DB_ADDR=db:6379
DB_PASS=

# PostgreSQL (local Docker)
PG_HOST=postgres
PG_PORT=5432
PG_USER=shortener
PG_PASS=shortener123
PG_NAME=shortenerdb